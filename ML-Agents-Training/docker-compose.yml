services:
  trainer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mlagents-trainer
    working_dir: /workspace
    volumes:
      - .:/workspace
    tty: true
    stdin_open: true
    ports:
      - "5004:5004" # Puerto por defecto de comunicación de ML-Agents
