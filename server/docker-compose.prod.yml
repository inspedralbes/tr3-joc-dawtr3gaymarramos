version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: fnaf-node-server
    restart: always
    environment:
      - PORT=11100
      - MONGO_URI=mongodb+srv://a24aymramagu_db_user:vkK7xRfOYDxDy096@jocaymar.3jopxj6.mongodb.net/?appName=JocAymar
    # No hace falta depends_on porque la DB está en la nube

  proxy:
    image: nginx:alpine
    container_name: fnaf-nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
